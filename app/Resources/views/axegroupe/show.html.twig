{% extends 'base.html.twig' %}

{% block body %}
    <h1>Axegroupe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ axeGroupe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ axeGroupe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ axeGroupe.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('axegroupe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('axegroupe_edit', { 'id': axeGroupe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
